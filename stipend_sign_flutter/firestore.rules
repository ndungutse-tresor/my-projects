rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Allow read/write for now (tighten before going to production)
      allow read, write: if true;
    }
  }
}
